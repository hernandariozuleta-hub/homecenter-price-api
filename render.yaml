services:
  - type: web
    name: homecenter-price-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ENABLE_PLAYWRIGHT
        value: "false"
    healthCheckPath: /health
